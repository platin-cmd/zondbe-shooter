using Unity.VisualScripting;
using UnityEngine;

public class BoostGateController : MonoBehaviour
{

    [Header("Скорости")]

    public float boostWalkSpeed = 40;

    public float boostSlideSpeed = 60;

    [Header("Настройки самих ворот")]

    public float boostTime = 10;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    public void OllisionEnter(Collision collision)
    {
        
    }
}
