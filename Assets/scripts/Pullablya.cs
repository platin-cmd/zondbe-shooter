using UnityEngine;

public enum PullType
{
    PullObjectToPlayer,
    PullPlayerToObject,
    BothCooking
}
public class Pullablya : MonoBehaviour
{
    public PullType pulltype;
  
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    

    
}
